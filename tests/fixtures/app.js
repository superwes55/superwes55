// Test fixture — intentionally contains no real credentials.
// Actual secret-pattern content is injected programmatically in conftest.py
// so it never appears as a literal in committed source files.
var config = {
  debug: true,
  version: "1.0.0",
};
console.log("app.js loaded");
